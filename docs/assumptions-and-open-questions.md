# Assumptions and open questions

Kept while working unattended, so they can be resolved together afterwards. Newest first within each phase. Each item says
what was assumed, why, and what to change if the assumption is wrong. Nothing here blocks the work; each is a default
that is easy to flip.

## Working method

- Work is on **stacked feature branches**, one per phase, each started from the previous one, with regular commits:
  `feature/phase-2-map-finish` → `feature/phase-3-cms` → `feature/phase-4-analytics` → `feature/phase-5-community` →
  `feature/phase-6-migration` → `feature/phase-7-deploy`. Nothing has been merged into `develop`, and nothing has been
  pushed. Merge in that order (each branch contains the ones before it).
- The live Elasticsearch cluster was used **read-only** (the API key cannot write). Nothing was written to any shared
  system. MySQL work uses a local scratch container only.

## Phase 2: map

| # | Assumption / gap | Default taken | Change it by |
|---|---|---|---|
| 2.1 | Vintage is the **default** basemap (the brief asks for a vintage motif). | `vintage` has `default: true` in `appsettings.Development.json` and Helm `map.basemaps`. | Move `default: true` to another basemap. |
| 2.2 | **GeoServer overlays** (1ATF topo, bases and towns): no GeoServer URL or layer names were provided. | `map.overlays` is empty; an example is in `values.yaml`. The Layers tab already lists overlays from config. | Give me the WMTS/WMS URL and layer names, or fill `map.overlays`. |
| 2.3 | **Elevation for 3D terrain**: the tile server has no DEM tileset. | Development uses public Terrarium tiles (AWS Open Data); Helm `map.terrain` is `null`, so the 3D switch is hidden in a deployed cluster. | Host a DEM (or accept AWS Terrarium for production) and set `map.terrain`. |
| 2.3b | The **vintage font package** (`deploy/map-styles/fonts`) has been generated but is installed on your tile server only when you copy it there. Until then labels in the deployed style fall back to whatever the server has. | The style's font stacks end in Noto Sans, which is already installed. | Copy `fonts/glyphs/*` (or `ttf/*`) into the server's fonts directory. |
| 2.4 | Courier Prime and Stardos Stencil (the site's own typewriter and stencil faces) **cannot label the map**: they lack about 80% of Vietnamese diacritics. | Noto Serif, IBM Plex Mono and Saira Stencil One (all OFL, full Vietnamese) are used instead. | Say if you want different fonts. |
| 2.5 | Contacts without a `Location` (79 of 6,236) are **not on the map** and their incident detail returns 404. | Excluded, as the legacy map did. | Tell me if they should be reachable some other way (for example from search). |
| 2.6 | `Source_Hyperlink` and `Hidden` **do not exist** in the live index. | The AWM link field is read if present, so it appears once reindexed. | Reindex with the field, or drop the link. |
| 2.7 | Legacy `?contact-filter=` links are **not** honoured (you said they need not be). | New readable parameters. | n/a |
| 2.8 | Unit tree labels (for example `D Coy`) come from `Title` + `ShortTypeName`; 34 parent units are synthetic groups named from `Path`. | Built as described in `UnitTreeBuilder`. | n/a |
| 2.9 | **Points of interest**: the legacy map only drew fire support bases (`FSB`, `FSPB`) and hid rows with `Visible = 'N'`. | All `Visible = 'Y'` rows are shown (97 FSB, 14 FSPB and 2 landing zones), on by default, under the contacts. Hidden rows stay in the table but never reach the API. | Change the filter in `PoiEndpoints`, or the default in `showPois`. |
| 2.10 | POI history text was HTML with external images (Google proxy URLs). | Reduced to plain text on import; images dropped. | Say if the images should be brought over. |
| 2.11 | 7 legacy POI rows have no name (all hidden fire support bases). | Skipped by the importer and counted in its report. | n/a |
| 2.12 | **Search**: incidents are found by every word in the report (top 8 by relevance, with the total), bases by name locally. Units and places are not searchable yet. | As described; shares the per-client rate limit (60 a minute) with the filter's report search. | Tell me which other things should be searchable (units, honour roll names, media). |
| 2.13 | A **scratch MySQL 5.7** container (`avw-scratch-mysql`, port 3308) holds the full legacy dump, which contains personal data. The dev MySQL 8.4 (`avw-dev-mysql-1`, port 3307) holds the schema plus the imported POIs. | Both run locally in Docker; nothing is exposed beyond localhost. | `docker rm -f avw-scratch-mysql` when the migration work is done. |

## Phase 3: content, Studio and media

| # | Assumption / gap | Default taken | Change it by |
|---|---|---|---|
| 3.1 | **Who can do what.** The plan says "authors submit; editors publish". | An author writes and edits **their own drafts** only. Once submitted for review they cannot edit (they can withdraw it back to draft). Editors and admins edit anything, publish, schedule, archive, create pages, choose web addresses and feature items on the home page. Only a draft or archived item can be deleted. | `ArticleService.Transitions` and `CanEdit`. |
| 3.2 | **Bylines are public.** The author's display name (from Keycloak `name`) is shown on articles and in the feed. | Yes. | Drop `AuthorName` from `ArticleCard`/`ArticleView` or let authors set a byline. |
| 3.3 | **Navigation** comes from the published page tree (top-level pages after "Stories"), not from a separate menu editor or a settings file. | Order is the page's "order among siblings", then title. Pages nest up to 5 deep; a page's address is its parent chain, and page slugs are unique across all pages. | Add a menu editor later if you want links that are not pages. |
| 3.4 | **Reserved first path segments** so a page cannot shadow the application: `api, studio, battlemap, articles, media, forbidden, feedback, feed, sitemap, login, logout, vendor, assets`. | A page with one of these names gets `-page` added, or is refused if typed. | `Slugs.Reserved` (API) and `RESERVED` in `cms-page.ts` (web); keep both in step. |
| 3.5 | **Revisions** are folded together: saves by the same person within 10 minutes share one revision, otherwise autosave would create one every few seconds. Restoring always makes a new revision. Nothing is ever pruned. | 10 minutes. | `ArticleService.RevisionWindow`; add pruning if history grows large. |
| 3.6 | **Preview** is inside the editor and shows the text after the server has cleaned it. Shareable draft preview links (in the plan) are **not** built. | In-editor only. | Needs a signed-token route; say if you want it. |
| 3.7 | **Public caching** is a short shared TTL (60 s, plus 5 min stale-while-revalidate), not the plan's tag-based invalidation. | A publish shows within a minute. | Add an invalidation hook when there is a cache in front. |
| 3.8 | **Media served by the API** at `/media` (ingress route added), immutable one-year caching. A picture's address is its content hash, so a *pending* picture is reachable by anyone who has the address, though nothing links to it. Only an **approved** picture can be an article's main picture; pending ones may be inserted in the body. | As described. | Serve only approved files (needs a lookup per request), or require approval before body use. |
| 3.9 | **Accepted pictures**: JPEG, PNG and WebP up to 25 MB and 100 megapixels, identified from their own bytes. GIF, HEIC and SVG are refused. Output is a progressive JPEG (quality 80, at most 1920×1080, never enlarged, EXIF and colour profiles stripped, transparency becomes white) plus a 480 px thumbnail. **Originals are not kept** (`keepOriginals` in Helm is not implemented). No `srcset` variants beyond the thumbnail. | As described. | `MediaOptions`, `MediaProcessor`. |
| 3.10 | **Stale temporary files**: the worker does not yet sweep `media/.incoming/` (a crashed upload can leave a file there). | Left for Phase 7. | n/a |
| 3.11 | **Image alt text** comes from the picture's caption when inserted; there is no per-image alt prompt, no `figure` with visible caption in the editor, and no Battle Map contact-card embed (a stretch item in the plan). | As described. | Extend `RichText`. |
| 3.12 | **Site name and description** ("Australia's Vietnam War", one description sentence) are set in code/config (`Site:Name`); structured data is `Article` only (no `Organization` or `BreadcrumbList`). | As described. | `Seo` service and `CmsEndpoints`. |
| 3.13 | **`PUBLIC_URL`** must be set on the web deployment (Helm `publicUrl`, default `https://<host>`) or canonical links, Open Graph URLs, `robots.txt` and the sitemap use the wrong address. | Helm derives it from `host`. | Set `publicUrl`. |
| 3.14 | **Search** covers published *articles* only (title, summary, text; simple substring match, newest first), not pages, and is not ranked. | As described. | Use Elasticsearch later if wanted. |
| 3.15 | **Feedback form**: messages are stored and shown to editors in the Studio (Feedback tab). **No email is sent** because no SMTP details were given. The optional email address is kept in plain text so an editor can reply, with no retention limit. Limit is 5 messages per visitor per 10 minutes; a hidden decoy field catches simple bots. | As described. | Provide SMTP settings for notifications; decide a retention period. |
| 3.16 | **WordPress content** (posts, pages, comments, feedback) is **not migrated**, as the plan says. Pages are written fresh in the Studio. | n/a | n/a |
| 3.17 | **Keycloak chart removed** at your instruction; the realm definition moved to `deploy/keycloak/avw-realm.json` (the local dev stack still imports it). Its placeholders are `${AVW_PUBLIC_URL}` and `${AVW_CLIENT_SECRET}`. | You apply it through your operator. | n/a |
| 3.18 | **Timestamps** now read back as UTC (`Z`), which the database does not record. All stored times are UTC. | Convention only, no schema change. | n/a |
| 3.19 | Sample rows were added to the **local dev MySQL** for checking (user 900, category 900, articles 9000-9004). They are not in any migration. | Left in the dev database. | `DELETE FROM articles WHERE Id BETWEEN 9000 AND 9010; DELETE FROM categories WHERE Id=900; DELETE FROM users WHERE Id=900;` |
| 3.20 | The Studio was checked in a real browser (Chrome) against a **mocked API** because signing in needs Keycloak. The API is covered by tests that fake the signed-in user. | As described. | Try it once with real logins. |

## Phase 4: timeline, charts and unit tracks

| # | Assumption / gap | Default taken | Change it by |
|---|---|---|---|
| 4.1 | **The unit tree and filters** were built in Phase 2, so this phase adds only the timeline, the unit track and the charts. | As described. | n/a |
| 4.2 | **`avw_tours` does not exist** on the live cluster (the legacy personnel charts read it). The three personnel charts are instead worked out from the nominal roll (`avw_nomroll`, 60,799 people): age at death, age at the start of each tour, and tours started by age band. The roll stores tour start dates as text (`dd/MM/yyyy`) and has no age fields, so ages are computed from birth date and that date; ages outside 14-90 are dropped as typing errors. | As described. | Reindex tours as their own index, or say what the personnel charts should show. |
| 4.3 | **Charts are calculated in the API from cached data, not by Elasticsearch aggregations.** The live mapping stores `Weapon_Effects` as a plain object (not `nested`), so per-weapon figures cannot be aggregated correctly there, and there is no `Hour` field. The API reads the contacts once (and the roll every 6 hours), caches them, and computes each chart from them. | As described. | Map `Weapon_Effects` as `nested` and add `Hour`, then move to aggregations if the data ever outgrows memory. |
| 4.4 | **One filter for every chart**: the browser sends the ids of the contacts the map is showing (an empty list gives an empty chart; no filter sends none, meaning every contact). Personnel charts ignore it. The 6,236 contacts includes the 79 with no location that the map omits, so an unfiltered chart counts a few more than the map shows. | As described. | `ChartFilter` and `AnalyticsEndpoints`. |
| 4.5 | **The 13 charts** follow the legacy ones (casualties by date and by hour, loss ratio by date and by hour, contacts by task by date and by hour, rounds and casualties by range, rounds by task, rounds per enemy casualty, and the three personnel charts). Changes: dates are grouped by month rather than day for the ratio, task and rounds-per-casualty charts (daily values are too noisy to read); only the eight commonest tasks, weapons and services get their own line ("Other" holds the rest, replacing the legacy fixed minimum count); range charts use 10 m steps and leave out uses with no recorded range. | As described. | `Charts.cs`. |
| 4.6 | **Loss ratio** is friendly casualties for each enemy casualty, counting none as one. The legacy formula (`Fr / (En \| 1) / ((Fr + En) \| 1)`, using a bitwise OR) was not reproduced because it does not mean what its name says. Each chart carries a note about this. | As described. | `Charts.LossRatio`. |
| 4.7 | **Timeline**: contacts per month along the bottom, all months grey and the ones passing the filters bright; the slider sets the date filter (the same one as in the Filters tab, so they always agree). Play slides a 3-month window on by a month every 0.4 s and stops at the end. It is a monthly overview, not the legacy daily waveform. | As described. | `timeline.ts`. |
| 4.8 | **Following a unit** works on the units chosen in the Filters tab (up to 6, each its own colour), joining their contacts in date order and marking the first and last, with previous/next buttons to step through them. The legacy way (an "eye" beside a unit name in an incident) is not built. A whole battalion is refused because its companies work side by side and the line would only tangle. | As described. | `track.ts`; add an eye button to the incident panel if wanted. |
| 4.9 | **Charts are drawn with Apache ECharts**, loaded only when needed. Every chart has a "show the figures as a table" alternative (up to 400 rows). There is no chart export (image or CSV) yet. | As described. | n/a |
| 4.10 | **Time of day** uses the hour in the contact's date-time as recorded (local time in Vietnam, stored without a zone). | As described. | n/a |

## Phase 5: community features

| # | Assumption / gap | Default taken | Change it by |
|---|---|---|---|
| 5.1 | **Rules for notes.** Editors' and admins' own notes and edits go live at once; everyone else's wait for approval. Until an editor approves a *change*, the public keeps seeing the last approved text (every save is a kept version). A rejected change leaves the old text up; a rejected first version never shows. Members may delete **their own** notes and comments (the plan says both "only privileged users may delete" and "so they can edit and delete their own notes"; I followed the second). | As described. | `NoteService`. |
| 5.2 | **Note text is plain text** (blank lines make paragraphs; nothing is ever treated as markup), up to 5,000 characters, with a title of up to 200. Legacy note bodies will be reduced to plain text on import (as the points of interest were). | As described. | n/a |
| 5.3 | **Comments** are shown at once (the legacy site had no approval for them), can be removed by their author or an editor, and can be closed per note by an editor. | As described. | n/a |
| 5.4 | **Pictures on incidents** use the Studio's upload pipeline and approval (editors' uploads go live, others wait). Views are **not** counted (the legacy `record-user-media-view` is dropped); likes are kept, one per person per picture, on approved pictures only. New uploads keep only an optional date taken; the legacy place (`Lat`/`Lon`) is kept for migrated pictures (see 6.7). | As described. | `IncidentMediaService`. |
| 5.5 | **Poppies (tributes)** are shown at once with no approval (the legacy table had none), limited to 10 an hour per person and 500 characters, and removable by the author or an editor. Old tributes had a host address stored; it is **not** migrated. | As described. | Add a moderation step if abuse appears. |
| 5.6 | **Casualty information** from members is stored for editors to read in the Studio (Moderation page) and act on by hand; nothing updates the honour roll automatically. Types: killed in action, died of wounds, wounded in action, missing, other. | As described. | `CasualtyService.Types`. |
| 5.7 | **Honour roll** = people on the nominal roll (`avw_nomroll`) with a date of death: 522 of 60,799. Names are shown as "First Second Third Surname". The roll has no link to incidents, so incident links live in a MySQL table (`casualty_links`, filled by the legacy `nomroll_contacts` import in Phase 6). Until then every incident shows an empty honour roll. Honours and the legacy extra fields (`nomroll_honours`, `nomroll_extra_fields`) are not shown. | As described. | n/a |
| 5.8 | **Portraits** are shown when a file `portraits/<service number>.jpg` exists in the media folder (put there by you: the legacy `honour-roll/` files). The roll's external `nomroll_personnel_media.ImageUrl` links are not used. | As described. | `ElasticsearchHonourRoll.PortraitFor`. |
| 5.9 | **Email to editors** (new note, changed note, new picture, casualty information) goes over SMTP from a queue, blind-copied to `Notifications:EditorEmails`. The plan reads the recipients from Keycloak role membership; that needs a service account and an admin API call, so a plain list is used for now. **No SMTP server was given**, so nothing is sent until `smtp.host`, `smtp.from` and `notifications.editorEmails` are set in the Helm values (and `Smtp__Password` in the secret). Meanwhile the API only logs that something is waiting. Uses .NET's built-in `SmtpClient` (no extra package). | As described. | Give me the SMTP details and recipients, or the Keycloak service-account details. |
| 5.10 | **Old content is claimed by verified email**: when someone signs in with a verified email whose hash matches, their migrated notes, comments, tributes and pictures become theirs. The importer (Phase 6) must store only the hash. | As described. | `LegacyContentLinker`. |
| 5.11 | **The rate limiter now runs after authentication**, so limits that are per person (uploads, tributes, writes) really are. Before this, per-person limits were per network address. | Fixed. | n/a |
| 5.12 | **Search** now also finds people on the honour roll by name or service number (up to 5), shown between bases and incidents. | As described. | n/a |
| 5.13 | **Not built:** a member's own profile page, a "my contributions" list, subscription or reply notifications to members, and a public author page. | Left out. | Say which you want. |

## Phase 6: migrating the Battle Map data

Run with `Avw.Migrator import-poi` and `Avw.Migrator import-community [--dry-run] [--media-root <folder>] [--skip-media]`. Both read the legacy MySQL named by `ConnectionStrings__Legacy`, match rows on their legacy id (so they can be repeated safely), and print counts only. On the real dump, into an empty database, the text import gave: **276** notes and comments (230 notes, 46 comments; 378 versions), **20** tributes, **8** casualty reports and **213** person-to-incident links; a second run changed nothing. The dev database was emptied again afterwards.

| # | Assumption / gap | Default taken | Change it by |
|---|---|---|---|
| 6.1 | **The Elasticsearch indexer is not built.** The plan had the API rebuild Elasticsearch from the legacy `es_*` views. The live cluster already holds those indexes (checked in Phase 2), and the API only reads them, so a second writer would risk overwriting good data. The reference tables (`contacts`, `units`, `nomroll_*` and so on) are therefore **not** copied into MySQL either. Only the POIs (Phase 2) and the community content are. | Skipped. | Tell me if the cluster will be rebuilt from the dump; then the indexer is a Phase 6b job. |
| 6.2 | **Pictures need the files.** The importer reads each picture's `Path` from a folder you give with `--media-root` (the legacy `incident-media/` uploads), runs it through the same pipeline as a new upload (so it is checked, straightened, resized to 1920x1080, made into a JPEG and a 480 px thumbnail, and stored under its hash) and skips any file it cannot find, so it can be run again when the files arrive. **Run on the real files** (`.ai/incident-media`, 523 files including thumbnails and `Thumbs.db`): all 353 rows found their file; 352 were imported in about 40 seconds (99 MB became 337 distinct pictures at 42 MB, none larger than 1904x1080), 1 was the same picture twice on one incident. A second run added nothing. Paths that try to leave the folder are refused. | Run when you have the folder. | `--media-root`. Set `Media__RootPath` to the shared media volume. |
| 6.3 | **Approval status:** legacy `1` is approved, `0` is pending and a negative value is rejected. Notes: 220 approved, 9 pending, 1 rejected. Pictures: 351 approved, 2 pending. An approved note shows its latest version. | As described. | `CommunityImporter`. |
| 6.4 | **Authors.** Each note, comment, tribute and picture keeps the author's display name (or "Member") and a **SHA-256 hash** of their email. No email address, host address or user id is stored. Anyone who signs in with the same verified email gets their old content back (see 5.10). Authors with no `wp_users` row are anonymous. | As described. | n/a |
| 6.5 | **Times.** Legacy `datetime` values have no zone; they are treated as UTC. | As described. | n/a |
| 6.6 | **Tributes.** Of 267, **247 were left without any message** (all anonymous; every tribute dates from December 2016 to June 2017), which look like one-click poppies. The new poppy needs a message, so these are **not** imported (20 are). Old poppies keep their author name, or the stored `AuthorName`. | Skipped the 247. | Say if you want them as counted poppies with no text. |
| 6.7 | **Pictures placed on the map.** The legacy pictures carry their own latitude and longitude, and some have no incident. They are kept: a picture with a position but no incident shows only on the map's picture layer (a new `GET /api/community-media?minLat=&minLon=&maxLat=&maxLon=`, at most 500 per box), and the moderation page hides the incident link for it. A position of exactly 0,0 or outside the earth is dropped. **The Battle Map now draws this layer** (see 6.15). Of the 350 pictures imported, **only 23 belong to an incident**; the rest have no incident, and 325 in all have a position, so the layer is the only place most of them can be seen. | Kept. | Ask me to add the picture layer to the map. |
| 6.8 | **Likes** are not stored per person: each picture keeps a count of its legacy likes (`LegacyLikes`, distinct people), added to new likes. Picture **views** and **tags** are dropped. | As described. | n/a |
| 6.9 | **Casualty reports.** All 8 have no comment text. They are imported with the comment "No details were given." because the service number, type and incident are still useful to an editor. Types not in the list become "Other". | As described. | n/a |
| 6.10 | **Casualty links** (which people were lost in which incident) come from the roll's own "Death Incident ID" together with `nomroll_contacts`, without duplicates: 213 links. Any of them that do not match a person on the live honour roll will simply not show. | As described. | n/a |
| 6.11 | **Long notes.** New notes are limited to 5,000 characters, but migrated notes are cut only at **16,000** (the database column holds about 64 KB). 10 of the 378 versions were longer than that and are cut there; the longest is over 50,000 characters. A member editing an old note over 5,000 characters must shorten it before saving. | As described. | I can widen the column to `mediumtext` and raise the limits if the cuts matter. |
| 6.12 | **Legacy uploader.** Migrated pictures are owned by one account (subject `legacy-import`, name "Legacy content") because the real uploaders have no account here. It is created on first use and has no roles. | As described. | n/a |
| 6.14 | **Unapproved duplicates.** Approval belongs to the picture file, not to where it is attached. Two legacy pictures that were not approved were the same files as approved ones, and importing them would have put them on new incidents unreviewed, so the importer leaves out an unapproved row whose file is already approved (reported as "not approved, and the same file is already approved elsewhere"). | As described. | `CommunityImporter.ImportMediaAsync`. |
| 6.15 | **The map's photo layer.** The Battle Map draws every approved picture with a position: a cream marker with a dark rim for one, a numbered disc where several crowd together (click one to zoom in until it breaks up), drawn above the contacts because there are few of them and one hidden under a contact marker would be lost. A "Photos" switch in the Layers tab turns it off (on by default). Clicking a marker opens a panel with the picture (480 px, click for full size), caption, credit, date taken, likes (members can like), and, for the 23 that belong to an incident, "Open the incident". The address carries `?picture=<id>` so a link reproduces it. All 325 are fetched once when the map opens (about 150 KB, compressed) rather than per view, because the set is small and only migrated pictures have a position (new uploads carry only a date, so it will not grow); the server caps a request at the newest 500, so if that ever grows the layer must fetch by area. There is no way to remove or edit a photo from its panel; an editor rejects one in the Studio's media library. | As described. | `photo-layers.ts`, `picture-panel.ts`. |
| 6.13 | **WordPress content** (posts, pages, comments, users, feedback) is not migrated, as planned. | Skipped. | n/a |

## Phase 7: deployment, checks and cutover

The procedures are in `runbook.md`.

| # | Assumption / gap | Default taken | Change it by |
|---|---|---|---|
| 7.1 | **Nothing has run on a real cluster.** (You have since given: RWO class `ceph-rbd`, ingress class `nginx`, x86-64. Now set in the values. The RWX class for the media volume is still unknown: `media.storage.pvc.storageClassName` is empty, which uses the cluster default.) The charts pass `helm lint` and `helm template`, and both images were built and run locally the way the charts run them (non-root, read-only root filesystem, dropped capabilities, with volumes), including the migrator and a picture import. The migration hook, the RWX/RWO volumes, the ingress, the Keycloak operator and the PVC permissions (`fsGroup`) are untested. | Expect a first-install shake-down. | Tell me what the cluster is (storage classes, ingress class, architecture) and I can tighten the values. |
| 7.2 | **Image platform.** The server image builds for the builder's platform, `linux-x64` unless BuildKit says `arm64` (`TARGETARCH`). x64 is your architecture, so this is what was built. It went from 783 MB to 366 MB by leaving out other platforms' native libraries. | As described. | `server/Dockerfile`. |
| 7.3 | **The migration tools no longer refer to the API project.** That made the migrator's publish fail, so the picture pipeline, plain-text cleaner, email hash and shared limits moved to a new `Avw.Core` library (same namespaces, nothing changed for callers). | Done. | n/a |
| 7.4 | **Content security policy starts in report-only mode.** I checked it *enforced* in Chrome on the home page, stories, feedback, Battle Map, incident panel and charts, with the real API and live data: no violations. I did not check the Studio (needs a sign-in) or articles with other embeds. So the chart default is `report-only`; switch to `enforce` after a quiet week in the logs (runbook section 5). | `csp.mode: report-only`. | `csp.mode` in the web values. |
| 7.5 | **Allowed hosts.** Map fetches are allowed to the tile host (`tiles.hosting.gradata.com.au`) and `s3.amazonaws.com` (the public elevation tiles, if you use them). Any other host (a GeoServer on another domain, another tile server) must be added to `csp.extraOrigins` or the map layer will be blocked once enforced. Images may come from any https host, so editors can link pictures from elsewhere; video only from YouTube (no-cookie) and Vimeo. | As described. | `csp.extraOrigins`; `img-src` in `web/src/security-headers.ts`. |
| 7.6 | **Fonts still load from Google Fonts** (Courier Prime, Source Sans 3, Stardos Stencil), which sends readers' addresses to Google and needs Google's hosts in the policy. Hosting them ourselves is a small change. | Left as it was. | Say if you want them self-hosted. |
| 7.7 | **Accessibility.** Automated checks (axe-core, WCAG 2.2 AA plus best practice) on seven page states found four kinds of problem, all fixed: paper-coloured text on the contact red was 4.3:1 (buttons now use a slightly darker red, `#b5331f`, 4.9:1; the map's contact red is unchanged); the stories list skipped a heading level; and the incident panel's scrolling area could not be reached by keyboard. **Not covered:** the Studio, signed-in views, keyboard and screen-reader use by a person, and phone screens. Automated tools find roughly a third of real problems. | Automated pass only. | You or a tester, using the manual list in the runbook. |
| 7.8 | **End-to-end tests are scripts, not part of CI.** `deploy/smoke/smoke.mjs` (22 checks) and `deploy/smoke/browser-checks.mjs` (Chrome: accessibility, policy violations, script errors, map drawing) need a running site with Elasticsearch and, for sign-in, Keycloak, which CI does not have. They pass locally against the real API and live data. Sign-in flows are covered only by the API's own tests (which fake the identity), not through Keycloak. | Run by hand at each release. | Give me a staging site and I can add a CI job that runs them. |
| 7.9 | **Load.** With your go-ahead I tested the Elasticsearch-backed requests against production, read-only, in steps of 1, 2, 4 and 8 connections for six seconds each, one endpoint at a time (numbers in the runbook). Nothing slowed or failed. My first run was wrong: search and honour-roll requests were being refused with 429 by the rate limiter and I had counted them as fast successes; I caught it by counting statuses and reran with the limit raised. | Done. | n/a |
| 7.16 | **Search limit.** Text searches (contacts, "find", honour roll) are limited to 60 a minute per client address, all kinds together, because each reaches Elasticsearch. That suits one person typing, but a school or office behind one address shares it. It is now a Helm value, `elasticsearch.searchPermitsPerMinute`. | 60. | The value. |
| 7.17 | **nginx upload size.** ingress-nginx refuses bodies over 1 MB unless told otherwise, which would have rejected every picture upload; the API chart now sets `proxy-body-size: 30m` on its ingress (uploads are limited to 25 MB). | Done. | `ingress.annotations`. |
| 7.18 | **A bug only real data found:** the MySQL connector cannot read a `date` column as a .NET `DateOnly`, so any query returning a picture with a date taken failed with a 500. The in-memory tests could not show it. Fixed with a value converter (no schema change). Nothing else in the model uses `DateOnly`. | Fixed. | n/a |
| 7.10 | **CI additions** (not yet run on GitHub): a gate on known-vulnerable NuGet and npm packages (both are clean today, so it will go red when an advisory appears, which is the point), extra chart renders (import job, volumes), and `kubeconform` schema checks on the rendered manifests. | As described. | `.github/workflows/ci.yaml`. |
| 7.11 | **The import is a manual Helm Job**, not a hook: `--set import.enabled=true` (a dry run by default). It needs `ConnectionStrings__Legacy` added to the API secret and, for pictures, the old `incident-media/` folder on a volume. | As described. | `import.*` values. |
| 7.12 | **Worker sweep.** Every 10 minutes it deletes files in `media/.incoming/` older than 6 hours, and API health markers older than 2 days. Files that are already in place (named by hash) are never touched. | As described. | `Media__IncomingMaxAgeMinutes`, `Media__HealthMarkerMaxAgeDays`. |
| 7.13 | **Old links are not redirected**, following your earlier answer. The legacy folders (`src/`, `wp-content/`, the root `Dockerfile`, `web.config`, `api`) stay until a week after cutover, then can be removed. | As described. | Ask for a redirect map. |
| 7.14 | **Still deferred:** the air and naval layers (Phase 2b), self-serve profile pages (5.13), chart export (4.9). | Left out. | Say which to build. |
| 7.15 | **A local component-style budget warning** (4 kB) was raised to 5 kB because the Battle Map's own style sheet was 14 bytes over. | Done. | `web/angular.json`. |

## Later phases

Added as the work proceeds.
