# Assumptions and open questions

Kept while working unattended, so they can be resolved together afterwards. Newest first within each phase. Each item says
what was assumed, why, and what to change if the assumption is wrong. Nothing here blocks the work; each is a default
that is easy to flip.

## Working method

- Work is on **stacked feature branches**, one per phase, each started from the previous one, with regular commits:
  `feature/phase-2-map-finish` → `feature/phase-3-cms` → `feature/phase-4-analytics` → `feature/phase-5-community` →
  `feature/phase-6-migration` → `feature/phase-7-deploy`. Nothing has been merged into `develop`, and nothing has been
  pushed. Merge in that order (each branch contains the ones before it).
- The live Elasticsearch cluster was used **read-only** (the API key cannot write). Nothing was written to any shared
  system. MySQL work uses a local scratch container only.

## Phase 2: map

| # | Assumption / gap | Default taken | Change it by |
|---|---|---|---|
| 2.1 | Vintage is the **default** basemap (the brief asks for a vintage motif). | `vintage` has `default: true` in `appsettings.Development.json` and Helm `map.basemaps`. | Move `default: true` to another basemap. |
| 2.2 | **GeoServer overlays** (1ATF topo, bases and towns): no GeoServer URL or layer names were provided. | `map.overlays` is empty; an example is in `values.yaml`. The Layers tab already lists overlays from config. | Give me the WMTS/WMS URL and layer names, or fill `map.overlays`. |
| 2.3 | **Elevation for 3D terrain**: the tile server has no DEM tileset. | Development uses public Terrarium tiles (AWS Open Data); Helm `map.terrain` is `null`, so the 3D switch is hidden in a deployed cluster. | Host a DEM (or accept AWS Terrarium for production) and set `map.terrain`. |
| 2.3b | The **vintage font package** (`deploy/map-styles/fonts`) has been generated but is installed on your tile server only when you copy it there. Until then labels in the deployed style fall back to whatever the server has. | The style's font stacks end in Noto Sans, which is already installed. | Copy `fonts/glyphs/*` (or `ttf/*`) into the server's fonts directory. |
| 2.4 | Courier Prime and Stardos Stencil (the site's own typewriter and stencil faces) **cannot label the map**: they lack about 80% of Vietnamese diacritics. | Noto Serif, IBM Plex Mono and Saira Stencil One (all OFL, full Vietnamese) are used instead. | Say if you want different fonts. |
| 2.5 | Contacts without a `Location` (79 of 6,236) are **not on the map** and their incident detail returns 404. | Excluded, as the legacy map did. | Tell me if they should be reachable some other way (for example from search). |
| 2.6 | `Source_Hyperlink` and `Hidden` **do not exist** in the live index. | The AWM link field is read if present, so it appears once reindexed. | Reindex with the field, or drop the link. |
| 2.7 | Legacy `?contact-filter=` links are **not** honoured (you said they need not be). | New readable parameters. | n/a |
| 2.8 | Unit tree labels (for example `D Coy`) come from `Title` + `ShortTypeName`; 34 parent units are synthetic groups named from `Path`. | Built as described in `UnitTreeBuilder`. | n/a |
| 2.9 | **Points of interest**: the legacy map only drew fire support bases (`FSB`, `FSPB`) and hid rows with `Visible = 'N'`. | All `Visible = 'Y'` rows are shown (97 FSB, 14 FSPB and 2 landing zones), on by default, under the contacts. Hidden rows stay in the table but never reach the API. | Change the filter in `PoiEndpoints`, or the default in `showPois`. |
| 2.10 | POI history text was HTML with external images (Google proxy URLs). | Reduced to plain text on import; images dropped. | Say if the images should be brought over. |
| 2.11 | 7 legacy POI rows have no name (all hidden fire support bases). | Skipped by the importer and counted in its report. | n/a |
| 2.12 | **Search**: incidents are found by every word in the report (top 8 by relevance, with the total), bases by name locally. Units and places are not searchable yet. | As described; shares the per-client rate limit (60 a minute) with the filter's report search. | Tell me which other things should be searchable (units, honour roll names, media). |
| 2.13 | A **scratch MySQL 5.7** container (`avw-scratch-mysql`, port 3308) holds the full legacy dump, which contains personal data. The dev MySQL 8.4 (`avw-dev-mysql-1`, port 3307) holds the schema plus the imported POIs. | Both run locally in Docker; nothing is exposed beyond localhost. | `docker rm -f avw-scratch-mysql` when the migration work is done. |

## Phase 3: content, Studio and media

| # | Assumption / gap | Default taken | Change it by |
|---|---|---|---|
| 3.1 | **Who can do what.** The plan says "authors submit; editors publish". | An author writes and edits **their own drafts** only. Once submitted for review they cannot edit (they can withdraw it back to draft). Editors and admins edit anything, publish, schedule, archive, create pages, choose web addresses and feature items on the home page. Only a draft or archived item can be deleted. | `ArticleService.Transitions` and `CanEdit`. |
| 3.2 | **Bylines are public.** The author's display name (from Keycloak `name`) is shown on articles and in the feed. | Yes. | Drop `AuthorName` from `ArticleCard`/`ArticleView` or let authors set a byline. |
| 3.3 | **Navigation** comes from the published page tree (top-level pages after "Stories"), not from a separate menu editor or a settings file. | Order is the page's "order among siblings", then title. Pages nest up to 5 deep; a page's address is its parent chain, and page slugs are unique across all pages. | Add a menu editor later if you want links that are not pages. |
| 3.4 | **Reserved first path segments** so a page cannot shadow the application: `api, studio, battlemap, articles, media, forbidden, feedback, feed, sitemap, login, logout, vendor, assets`. | A page with one of these names gets `-page` added, or is refused if typed. | `Slugs.Reserved` (API) and `RESERVED` in `cms-page.ts` (web); keep both in step. |
| 3.5 | **Revisions** are folded together: saves by the same person within 10 minutes share one revision, otherwise autosave would create one every few seconds. Restoring always makes a new revision. Nothing is ever pruned. | 10 minutes. | `ArticleService.RevisionWindow`; add pruning if history grows large. |
| 3.6 | **Preview** is inside the editor and shows the text after the server has cleaned it. Shareable draft preview links (in the plan) are **not** built. | In-editor only. | Needs a signed-token route; say if you want it. |
| 3.7 | **Public caching** is a short shared TTL (60 s, plus 5 min stale-while-revalidate), not the plan's tag-based invalidation. | A publish shows within a minute. | Add an invalidation hook when there is a cache in front. |
| 3.8 | **Media served by the API** at `/media` (ingress route added), immutable one-year caching. A picture's address is its content hash, so a *pending* picture is reachable by anyone who has the address, though nothing links to it. Only an **approved** picture can be an article's main picture; pending ones may be inserted in the body. | As described. | Serve only approved files (needs a lookup per request), or require approval before body use. |
| 3.9 | **Accepted pictures**: JPEG, PNG and WebP up to 25 MB and 100 megapixels, identified from their own bytes. GIF, HEIC and SVG are refused. Output is a progressive JPEG (quality 80, at most 1920×1080, never enlarged, EXIF and colour profiles stripped, transparency becomes white) plus a 480 px thumbnail. **Originals are not kept** (`keepOriginals` in Helm is not implemented). No `srcset` variants beyond the thumbnail. | As described. | `MediaOptions`, `MediaProcessor`. |
| 3.10 | **Stale temporary files**: the worker does not yet sweep `media/.incoming/` (a crashed upload can leave a file there). | Left for Phase 7. | n/a |
| 3.11 | **Image alt text** comes from the picture's caption when inserted; there is no per-image alt prompt, no `figure` with visible caption in the editor, and no Battle Map contact-card embed (a stretch item in the plan). | As described. | Extend `RichText`. |
| 3.12 | **Site name and description** ("Australia's Vietnam War", one description sentence) are set in code/config (`Site:Name`); structured data is `Article` only (no `Organization` or `BreadcrumbList`). | As described. | `Seo` service and `CmsEndpoints`. |
| 3.13 | **`PUBLIC_URL`** must be set on the web deployment (Helm `publicUrl`, default `https://<host>`) or canonical links, Open Graph URLs, `robots.txt` and the sitemap use the wrong address. | Helm derives it from `host`. | Set `publicUrl`. |
| 3.14 | **Search** covers published *articles* only (title, summary, text; simple substring match, newest first), not pages, and is not ranked. | As described. | Use Elasticsearch later if wanted. |
| 3.15 | **Feedback form**: messages are stored and shown to editors in the Studio (Feedback tab). **No email is sent** because no SMTP details were given. The optional email address is kept in plain text so an editor can reply, with no retention limit. Limit is 5 messages per visitor per 10 minutes; a hidden decoy field catches simple bots. | As described. | Provide SMTP settings for notifications; decide a retention period. |
| 3.16 | **WordPress content** (posts, pages, comments, feedback) is **not migrated**, as the plan says. Pages are written fresh in the Studio. | n/a | n/a |
| 3.17 | **Keycloak chart removed** at your instruction; the realm definition moved to `deploy/keycloak/avw-realm.json` (the local dev stack still imports it). Its placeholders are `${AVW_PUBLIC_URL}` and `${AVW_CLIENT_SECRET}`. | You apply it through your operator. | n/a |
| 3.18 | **Timestamps** now read back as UTC (`Z`), which the database does not record. All stored times are UTC. | Convention only, no schema change. | n/a |
| 3.19 | Sample rows were added to the **local dev MySQL** for checking (user 900, category 900, articles 9000-9004). They are not in any migration. | Left in the dev database. | `DELETE FROM articles WHERE Id BETWEEN 9000 AND 9010; DELETE FROM categories WHERE Id=900; DELETE FROM users WHERE Id=900;` |
| 3.20 | The Studio was checked in a real browser (Chrome) against a **mocked API** because signing in needs Keycloak. The API is covered by tests that fake the signed-in user. | As described. | Try it once with real logins. |

## Phase 4: timeline, charts and unit tracks

| # | Assumption / gap | Default taken | Change it by |
|---|---|---|---|
| 4.1 | **The unit tree and filters** were built in Phase 2, so this phase adds only the timeline, the unit track and the charts. | As described. | n/a |
| 4.2 | **`avw_tours` does not exist** on the live cluster (the legacy personnel charts read it). The three personnel charts are instead worked out from the nominal roll (`avw_nomroll`, 60,799 people): age at death, age at the start of each tour, and tours started by age band. The roll stores tour start dates as text (`dd/MM/yyyy`) and has no age fields, so ages are computed from birth date and that date; ages outside 14-90 are dropped as typing errors. | As described. | Reindex tours as their own index, or say what the personnel charts should show. |
| 4.3 | **Charts are calculated in the API from cached data, not by Elasticsearch aggregations.** The live mapping stores `Weapon_Effects` as a plain object (not `nested`), so per-weapon figures cannot be aggregated correctly there, and there is no `Hour` field. The API reads the contacts once (and the roll every 6 hours), caches them, and computes each chart from them. | As described. | Map `Weapon_Effects` as `nested` and add `Hour`, then move to aggregations if the data ever outgrows memory. |
| 4.4 | **One filter for every chart**: the browser sends the ids of the contacts the map is showing (an empty list gives an empty chart; no filter sends none, meaning every contact). Personnel charts ignore it. The 6,236 contacts includes the 79 with no location that the map omits, so an unfiltered chart counts a few more than the map shows. | As described. | `ChartFilter` and `AnalyticsEndpoints`. |
| 4.5 | **The 13 charts** follow the legacy ones (casualties by date and by hour, loss ratio by date and by hour, contacts by task by date and by hour, rounds and casualties by range, rounds by task, rounds per enemy casualty, and the three personnel charts). Changes: dates are grouped by month rather than day for the ratio, task and rounds-per-casualty charts (daily values are too noisy to read); only the eight commonest tasks, weapons and services get their own line ("Other" holds the rest, replacing the legacy fixed minimum count); range charts use 10 m steps and leave out uses with no recorded range. | As described. | `Charts.cs`. |
| 4.6 | **Loss ratio** is friendly casualties for each enemy casualty, counting none as one. The legacy formula (`Fr / (En \| 1) / ((Fr + En) \| 1)`, using a bitwise OR) was not reproduced because it does not mean what its name says. Each chart carries a note about this. | As described. | `Charts.LossRatio`. |
| 4.7 | **Timeline**: contacts per month along the bottom, all months grey and the ones passing the filters bright; the slider sets the date filter (the same one as in the Filters tab, so they always agree). Play slides a 3-month window on by a month every 0.4 s and stops at the end. It is a monthly overview, not the legacy daily waveform. | As described. | `timeline.ts`. |
| 4.8 | **Following a unit** works on the units chosen in the Filters tab (up to 6, each its own colour), joining their contacts in date order and marking the first and last, with previous/next buttons to step through them. The legacy way (an "eye" beside a unit name in an incident) is not built. A whole battalion is refused because its companies work side by side and the line would only tangle. | As described. | `track.ts`; add an eye button to the incident panel if wanted. |
| 4.9 | **Charts are drawn with Apache ECharts**, loaded only when needed. Every chart has a "show the figures as a table" alternative (up to 400 rows). There is no chart export (image or CSV) yet. | As described. | n/a |
| 4.10 | **Time of day** uses the hour in the contact's date-time as recorded (local time in Vietnam, stored without a zone). | As described. | n/a |

## Phase 5: community features

| # | Assumption / gap | Default taken | Change it by |
|---|---|---|---|
| 5.1 | **Rules for notes.** Editors' and admins' own notes and edits go live at once; everyone else's wait for approval. Until an editor approves a *change*, the public keeps seeing the last approved text (every save is a kept version). A rejected change leaves the old text up; a rejected first version never shows. Members may delete **their own** notes and comments (the plan says both "only privileged users may delete" and "so they can edit and delete their own notes"; I followed the second). | As described. | `NoteService`. |
| 5.2 | **Note text is plain text** (blank lines make paragraphs; nothing is ever treated as markup), up to 5,000 characters, with a title of up to 200. Legacy note bodies will be reduced to plain text on import (as the points of interest were). | As described. | n/a |
| 5.3 | **Comments** are shown at once (the legacy site had no approval for them), can be removed by their author or an editor, and can be closed per note by an editor. | As described. | n/a |
| 5.4 | **Pictures on incidents** use the Studio's upload pipeline and approval (editors' uploads go live, others wait). Views are **not** counted (the legacy `record-user-media-view` is dropped); likes are kept, one per person per picture, on approved pictures only. New uploads keep only an optional date taken; the legacy place (`Lat`/`Lon`) is kept for migrated pictures (see 6.7). | As described. | `IncidentMediaService`. |
| 5.5 | **Poppies (tributes)** are shown at once with no approval (the legacy table had none), limited to 10 an hour per person and 500 characters, and removable by the author or an editor. Old tributes had a host address stored; it is **not** migrated. | As described. | Add a moderation step if abuse appears. |
| 5.6 | **Casualty information** from members is stored for editors to read in the Studio (Moderation page) and act on by hand; nothing updates the honour roll automatically. Types: killed in action, died of wounds, wounded in action, missing, other. | As described. | `CasualtyService.Types`. |
| 5.7 | **Honour roll** = people on the nominal roll (`avw_nomroll`) with a date of death: 522 of 60,799. Names are shown as "First Second Third Surname" (the nominal roll list writes them "Surname, First Second Third": R.20). The roll has no link to incidents, so incident links live in a MySQL table (`casualty_links`, filled by the legacy `nomroll_contacts` import in Phase 6). Until then every incident shows an empty honour roll. Honours and the legacy extra fields (`nomroll_honours`, `nomroll_extra_fields`) are not shown. | As described. | n/a |
| 5.8 | **Portraits** are shown when a file `portraits/<service number>.jpg` exists in the media folder (put there by you: the legacy `honour-roll/` files). The roll's external `nomroll_personnel_media.ImageUrl` links are not used. | As described. | `ElasticsearchHonourRoll.PortraitFor`. |
| 5.9 | **Email to editors** (new note, changed note, new picture, casualty information) is **optional, and off for now** (your decision). Nothing has to be configured for the site to run: with `smtp.host`, `smtp.from` or `notifications.editorEmails` unset, the API logs one line at start-up ("Email notifications are off ...") and one per waiting item, and editors see everything on the Studio's Moderation page. To turn it on later, set those three in the Helm values (`smtp.user` with `Smtp__Password` in the secret if the server needs a login) and `notifications.siteUrl` so the links work. Messages go from a queue, blind-copied to the list. The plan reads the recipients from Keycloak role membership; that needs a service account and an admin API call, so a plain list is used. Uses .NET's built-in `SmtpClient`. | Off. | Set the values when you have the SMTP details. |
| 5.10 | **Old content is claimed by verified email**: when someone signs in with a verified email whose hash matches, their migrated notes, comments, tributes and pictures become theirs. The importer (Phase 6) must store only the hash. | As described. | `LegacyContentLinker`. |
| 5.11 | **The rate limiter now runs after authentication**, so limits that are per person (uploads, tributes, writes) really are. Before this, per-person limits were per network address. | Fixed. | n/a |
| 5.12 | **Search** now also finds people on the honour roll by name or service number (up to 5), shown between bases and incidents. | As described. | n/a |
| 5.13 | **Not built:** a member's own profile page, a "my contributions" list, subscription or reply notifications to members, and a public author page. | Left out. | Say which you want. |

## Phase 6: migrating the Battle Map data

Run with `Avw.Migrator import-poi` and `Avw.Migrator import-community [--dry-run] [--media-root <folder>] [--skip-media]`. Both read the legacy MySQL named by `ConnectionStrings__Legacy`, match rows on their legacy id (so they can be repeated safely), and print counts only. On the real dump, into an empty database, the text import gave: **276** notes and comments (230 notes, 46 comments; 378 versions, none cut), **267** poppies, **8** casualty reports and **213** person-to-incident links; a second run changed nothing. The dev database was emptied again afterwards.

| # | Assumption / gap | Default taken | Change it by |
|---|---|---|---|
| 6.1 | **The Elasticsearch indexer is not built.** The plan had the API rebuild Elasticsearch from the legacy `es_*` views. The live cluster already holds those indexes (checked in Phase 2), and the API only reads them, so a second writer would risk overwriting good data. The reference tables (`contacts`, `units`, `nomroll_*` and so on) are therefore **not** copied into MySQL either. Only the POIs (Phase 2) and the community content are. | Skipped. | Tell me if the cluster will be rebuilt from the dump; then the indexer is a Phase 6b job. |
| 6.2 | **Pictures need the files.** The importer reads each picture's `Path` from a folder you give with `--media-root` (the legacy `incident-media/` uploads), runs it through the same pipeline as a new upload (so it is checked, straightened, resized to 1920x1080, made into a JPEG and a 480 px thumbnail, and stored under its hash) and skips any file it cannot find, so it can be run again when the files arrive. **Run on the real files** (`.ai/incident-media`, 523 files including thumbnails and `Thumbs.db`): all 353 rows found their file; 350 were imported in about 40 seconds (99 MB became 337 distinct pictures at 42 MB, none larger than 1904x1080), 1 was the same picture twice on one incident, and 2 were rejected by a moderator and are left out. A second run added nothing. Paths that try to leave the folder are refused. | Run when you have the folder. | `--media-root`. Set `Media__RootPath` to the shared media volume. |
| 6.3 | **Approval status** (corrected). The old site's values, for notes and pictures alike, are **1 approved, 0 rejected by a moderator, -1 waiting for one** (from `incident-note.js` and `upload-incident-media.php`; anyone but an editor or admin starts at -1). An earlier version of the importer had 0 and -1 the wrong way round, which would have shown the 9 rejected notes as waiting for approval. In the 2017 dump: notes 220 approved, **1 waiting, 9 rejected**; pictures 351 approved, **2 rejected**. Rejected notes are kept, visible only to their author and editors, as on the old site. Rejected pictures are not imported (their files are not kept). An approved note shows its latest version. | As described. | `CommunityImporter.NoteStatus`, `PictureStatus`. |
| 6.4 | **Authors.** Each note, comment, tribute and picture keeps the author's display name (or "Member") and a **SHA-256 hash** of their email. No email address, host address or user id is stored. Anyone who signs in with the same verified email gets their old content back (see 5.10). Authors with no `wp_users` row are anonymous. | As described. | n/a |
| 6.5 | **Times.** Legacy `datetime` values have no zone; they are treated as UTC. | As described. | n/a |
| 6.6 | **Poppies.** All 267 are imported (your decision). 247 were laid with no message (all anonymous, one click, December 2016 to June 2017): they are kept as poppies with no words, shown as "Laid a poppy." and counted, and listed after the poppies that have words. They are for 197 people, 196 of whom are on the honour roll; the one wordless poppy for someone who is not on the roll cannot be seen. New poppies still need a message. Old poppies keep the author's name, or "Member". | As described. | `CommunityImporter.ImportTributesAsync`, `TributeService.ListAsync`. |
| 6.7 | **Pictures placed on the map.** The legacy pictures carry their own latitude and longitude, and some have no incident. They are kept: a picture with a position but no incident shows only on the map's picture layer (a new `GET /api/community-media?minLat=&minLon=&maxLat=&maxLon=`, at most 500 per box), and the moderation page hides the incident link for it. A position of exactly 0,0 or outside the earth is dropped. **The Battle Map now draws this layer** (see 6.15). Of the 350 pictures imported, **only 23 belong to an incident**; the rest have no incident, and 325 in all have a position, so the layer is the only place most of them can be seen. | Kept. | Ask me to add the picture layer to the map. |
| 6.8 | **Likes** are not stored per person: each picture keeps a count of its legacy likes (`LegacyLikes`, distinct people), added to new likes. Picture **views** and **tags** are dropped. | As described. | n/a |
| 6.9 | **Casualty reports.** All 8 have no comment text. They are imported with the comment "No details were given." because the service number, type and incident are still useful to an editor. Types not in the list become "Other". | As described. | n/a |
| 6.10 | **Casualty links** (which people were lost in which incident) come from the roll's own "Death Incident ID" together with `nomroll_contacts`, without duplicates: 213 links. Any of them that do not match a person on the live honour roll will simply not show. | As described. | n/a |
| 6.11 | **Long notes** (your decision: keep them whole). The note text column (`incident_note_versions.Body`) was already a `text` (65,535 bytes), so no schema change was needed; the importer now cuts only at 65,000 bytes, which nothing reaches. Checked by importing the real dump into MySQL: all 378 versions are kept, the longest is 54,900 characters, and 49 are over 5,000. New notes are still limited to 5,000 characters. A member can edit an old note that is longer than that, but not make it longer than it is (the edit box and the server both allow up to its current length), so adding words means removing as many. | As described. | `CommunityImporter.MaxLegacyBodyBytes`, `NoteService.UpdateAsync`. |
| 6.12 | **Legacy uploader.** Migrated pictures are owned by one account (subject `legacy-import`, name "Legacy content") because the real uploaders have no account here. It is created on first use and has no roles. | As described. | n/a |
| 6.14 | **Waiting duplicates.** Approval belongs to the picture file, not to where it is attached. A row still waiting for a moderator whose file is already approved would put that file on a new incident or place unreviewed, so the importer leaves it out (reported as "not approved, and the same file is already approved elsewhere"). Nothing in the 2017 dump is waiting; the two pictures that first showed the problem turned out to be rejected ones (see 6.3), which are skipped outright. | As described. | `CommunityImporter.ImportMediaAsync`. |
| 6.15 | **The map's photo layer.** The Battle Map draws every approved picture with a position: a cream marker with a dark rim for one, a numbered disc where several crowd together (click one to zoom in until it breaks up), drawn above the contacts because there are few of them and one hidden under a contact marker would be lost. A "Photos" switch in the Layers tab turns it off (on by default). Clicking a marker opens a panel with the picture (480 px, click for full size), caption, credit, date taken, likes (members can like), and, for the 23 that belong to an incident, "Open the incident". The address carries `?picture=<id>` so a link reproduces it. All 325 are fetched once when the map opens (about 150 KB, compressed) rather than per view, because the set is small and only migrated pictures have a position (new uploads carry only a date, so it will not grow); the server caps a request at the newest 500, so if that ever grows the layer must fetch by area. There is no way to remove or edit a photo from its panel; an editor rejects one in the Studio's media library. | As described. | `photo-layers.ts`, `picture-panel.ts`. |
| 6.16 | **Nothing here depends on the legacy MySQL's views or stored procedures.** The API, worker and web never connect to the legacy database; only the one-off importer does, and it reads 11 base tables (`wp_users`, `incident_notes`, `incident_note_versions`, `incident_note_comments`, `incident_media`, `incident_media_likes`, `personal_tributes`, `casualty_incident_submissions`, `nomroll_personnel`, `nomroll_contacts`, `poi`). The legacy database has 17 views and 39 routines. **Indirect dependence:** the `es_*` views define the documents in Elasticsearch, so whatever keeps the cluster up to date is still built on them; this project only reads Elasticsearch and has no indexer (6.1). **Logic that lived in procedures and was rewritten:** the analytics procedures became the 13 charts, computed from cached Elasticsearch data. Two were compared with the procedures' own SQL: battle damage by date matches the visible contacts exactly on all 1,803 days in all four series. The old procedures, called with blank filters, silently leave out any contact with no unit or no operation, so the old site's totals are lower (for example 1,692 of the 6,236 contacts are missing from its incident-frequency counts, and 372 against 390 friendly killed); the new charts count them all. The other 11 charts were not compared. The note, comment, picture, poppy and casualty procedures were replaced by C# services; I took the approval values from the old front end and upload scripts (6.3) but did not diff the procedure code. | As described. | Ask me to compare more charts or read the note and approval procedures. |
| 6.17 | **The 2017 dump is out of date.** Elasticsearch's `avw_incident_media` holds **493** pictures: all 353 in the dump plus **140 added between March and December 2018** (126 with a position). All 493 have their file in `.ai/incident-media`, so the folder is current but the database dump is not: the 140 have no rows to import and are ignored. **Notes are current:** the `avw_incident_notes` index (added later, presumably from the live database) holds the same 230 notes as the dump, with identical approval values (220 approved, 9 rejected, 1 waiting), incidents and created and modified times; the newest edit is April 2017. It has only each note's latest text, with no comments or history, so it cannot replace the dump for those. Poppies, casualty reports, comments and users have no index, so whether they moved on after 2017 is unknown. The media documents have no approval status or attribution, so that index cannot stand in for a dump either; if `avw_incident_media` were re-indexed from the current `es_incident_media` view (which does select both), the 140 could be imported from it, though without likes or authors' emails. | Import from the dump we have. | Give me a current dump of the legacy database (or read-only access) and re-run; every step is repeatable. |
| 6.18 | **Pictures are not written to Elasticsearch.** The old site, on each upload, called a procedure (a MySQL row) and put a document in the `avw_incident_media` index, and updated the document when a moderator approved it. The new site writes MySQL only: `media_assets` (the file) and `incident_media` (its incident, place, date and author hash), plus likes. Nothing in the new site reads that index (an incident's pictures, the map layer and the moderation queue all come from MySQL, and the API's key is read-only). So the index stops growing from new-site uploads, and anything else that reads it would not see them (I do not know of anything). The old site's search box could also find pictures by their description; that is not built. | As described. | Say if something else reads the index, or if picture search is wanted. |
| 6.13 | **WordPress content** (posts, pages, comments, users, feedback) is not migrated, as planned. | Skipped. | n/a |

## Phase 7: deployment, checks and cutover

The procedures are in `runbook.md`.

| # | Assumption / gap | Default taken | Change it by |
|---|---|---|---|
| 7.1 | **Nothing has run on a real cluster.** (You have since given: RWO class `ceph-rbd`, ingress class `nginx`, x86-64. Now set in the values. The RWX class for the media volume is still unknown: `media.storage.pvc.storageClassName` is empty, which uses the cluster default.) The charts pass `helm lint` and `helm template`, and both images were built and run locally the way the charts run them (non-root, read-only root filesystem, dropped capabilities, with volumes), including the migrator and a picture import. The migration hook, the RWX/RWO volumes, the ingress, the Keycloak operator and the PVC permissions (`fsGroup`) are untested. | Expect a first-install shake-down. | Tell me what the cluster is (storage classes, ingress class, architecture) and I can tighten the values. |
| 7.2 | **Image platform.** The server image builds for the builder's platform, `linux-x64` unless BuildKit says `arm64` (`TARGETARCH`). x64 is your architecture, so this is what was built. It went from 783 MB to 366 MB by leaving out other platforms' native libraries. | As described. | `server/Dockerfile`. |
| 7.3 | **The migration tools no longer refer to the API project.** That made the migrator's publish fail, so the picture pipeline, plain-text cleaner, email hash and shared limits moved to a new `Avw.Core` library (same namespaces, nothing changed for callers). | Done. | n/a |
| 7.4 | **Content security policy starts in report-only mode.** I checked it *enforced* in Chrome on the home page, stories, feedback, Battle Map, incident panel and charts, with the real API and live data: no violations. I did not check the Studio (needs a sign-in) or articles with other embeds. So the chart default is `report-only`; switch to `enforce` after a quiet week in the logs (runbook section 5). | `csp.mode: report-only`. | `csp.mode` in the web values. |
| 7.5 | **Allowed hosts.** Map fetches are allowed to the tile host (`tiles.hosting.gradata.com.au`), the satellite imagery (`server.arcgisonline.com`), the GeoServer overlay (`geoserver.hosting.gradata.com.au`) and `s3.amazonaws.com` (the public elevation tiles, if you use them). MapLibre fetches raster tiles as well as vector ones, so these fall under `connect-src`, not only `img-src`. `ng serve` uses the same list when `CSP_EXTRA_ORIGINS` is unset (`DEV_EXTRA_ORIGINS`). Any other host (another GeoServer or tile server) must be added to `csp.extraOrigins` or the map layer will be blocked once enforced. Images may come from any https host, so editors can link pictures from elsewhere; video only from YouTube (no-cookie) and Vimeo. | As described. | `csp.extraOrigins`; `img-src` in `web/src/security-headers.ts`. |
| 7.6 | **Fonts are self-hosted** (your decision). Courier Prime, Source Sans 3 and Stardos Stencil are the site's own files (`web/src/fonts.css`, made from the `@fontsource` packages; SIL Open Font License): only the weights the site uses, Latin and Latin Extended, and Vietnamese for the body face (place names such as Đường need it), woff2 only. That is 18 files and 336 KB, and a browser downloads one only when a page uses a character in its range. The policy no longer allows Google's hosts. Checked in Chrome: the home page contacts no host but the site, all font files come from it, and Vietnamese text fetches the Vietnamese file. To add a weight or a language, extend the list and regenerate the file. | Done. | `web/src/fonts.css`. |
| 7.7 | **Accessibility.** Automated checks (axe-core, WCAG 2.2 AA plus best practice) on seven page states found four kinds of problem, all fixed: paper-coloured text on the contact red was 4.3:1 (buttons now use a slightly darker red, `#b5331f`, 4.9:1; the map's contact red is unchanged); the stories list skipped a heading level; and the incident panel's scrolling area could not be reached by keyboard. **Not covered:** the Studio, signed-in views, keyboard and screen-reader use by a person, and phone screens. Automated tools find roughly a third of real problems. | Automated pass only. | You or a tester, using the manual list in the runbook. |
| 7.8 | **End-to-end tests are scripts, not part of CI.** `deploy/smoke/smoke.mjs` (22 checks) and `deploy/smoke/browser-checks.mjs` (Chrome: accessibility, policy violations, script errors, map drawing) need a running site with Elasticsearch and, for sign-in, Keycloak, which CI does not have. They pass locally against the real API and live data. Sign-in was checked by hand against the dev Keycloak (a member; and an editor, including enrolling an authenticator and signing in with a code), which worked, but that is not a repeatable script, and the API's own tests fake the identity. | Run by hand at each release. | Give me a staging site and I can add a CI job that runs them. |
| 7.9 | **Load.** With your go-ahead I tested the Elasticsearch-backed requests against production, read-only, in steps of 1, 2, 4 and 8 connections for six seconds each, one endpoint at a time (numbers in the runbook). Nothing slowed or failed. My first run was wrong: search and honour-roll requests were being refused with 429 by the rate limiter and I had counted them as fast successes; I caught it by counting statuses and reran with the limit raised. | Done. | n/a |
| 7.16 | **Search limit.** Text searches of contacts and "find" are limited to 60 a minute per client address, all kinds together, because each reaches Elasticsearch. The honour roll (R.20) is in MySQL and has the notes-and-pictures allowance (120 a minute) instead. That suits one person typing, but a school or office behind one address shares it. It is now a Helm value, `elasticsearch.searchPermitsPerMinute`. | 60. | The value. |
| 7.17 | **nginx upload size.** ingress-nginx refuses bodies over 1 MB unless told otherwise, which would have rejected every picture upload; the API chart now sets `proxy-body-size: 30m` on its ingress (uploads are limited to 25 MB). | Done. | `ingress.annotations`. |
| 7.18 | **A bug only real data found:** the MySQL connector cannot read a `date` column as a .NET `DateOnly`, so any query returning a picture with a date taken failed with a 500. The in-memory tests could not show it. Fixed with a value converter (no schema change). Nothing else in the model uses `DateOnly`. | Fixed. | n/a |
| 7.10 | **CI additions** (not yet run on GitHub): a gate on known-vulnerable NuGet and npm packages (both are clean today, so it will go red when an advisory appears, which is the point), extra chart renders (import job, volumes), and `kubeconform` schema checks on the rendered manifests. | As described. | `.github/workflows/ci.yaml`. |
| 7.11 | **The import is a manual Helm Job**, not a hook: `--set import.enabled=true` (a dry run by default). It needs `ConnectionStrings__Legacy` added to the API secret and, for pictures, the old `incident-media/` folder on a volume. | As described. | `import.*` values. |
| 7.12 | **Worker sweep.** Every 10 minutes it deletes files in `media/.incoming/` older than 6 hours, and API health markers older than 2 days. Files that are already in place (named by hash) are never touched. | As described. | `Media__IncomingMaxAgeMinutes`, `Media__HealthMarkerMaxAgeDays`. |
| 7.13 | **Old links are not redirected**, following your earlier answer. The legacy folders (`src/`, `wp-content/`, the root `Dockerfile`, `web.config`, `api`) stay until a week after cutover, then can be removed. | As described. | Ask for a redirect map. |
| 7.14 | **Still deferred** (kept deferred, as you said): the air and naval layers (Phase 2b), self-serve profile pages (5.13), chart export (4.9). | Left out. | Say when. |
| 7.15 | **A local component-style budget warning** (4 kB) was raised to 5 kB because the Battle Map's own style sheet was 14 bytes over. | Done. | `web/angular.json`. |

## Notes and pictures in Elasticsearch: decided against

I built a service that copied notes and pictures into Elasticsearch as they changed (a database outbox, a worker that wrote them in bulk, and the live indexes' field shapes). It worked (19 end-to-end checks against a local Elasticsearch, including an outage), and was then **removed at your direction**: the only reasons for those indexes were search and display, and MySQL 8.4 does both.

| # | Assumption / gap | Default taken | Change it by |
|---|---|---|---|
| 8.1 | **Notes and pictures are not indexed in Elasticsearch.** The new site reads and writes them in MySQL only (the old site wrote both, and the live `avw_incident_notes` and `avw_incident_media` indexes stay as they are, no longer fed by this project). **Display** is MySQL only (incident panels, the map's photo layer, moderation). **Search:** the old site's notes and pictures search was never finished (selecting a result called `alert(...)`), and the new search box covers bases, people and reports only. MySQL 8.4 can do it: tried on the real data with temporary full-text indexes (since dropped), approved note text only, whole-word matching so plurals need a prefix search (`claymor*` found 29 notes where plain `claymore` found 11), phrases (`"Nui Dat"` 61), and accent-insensitive (`Phuoc` matches `Phước`, `Duong` matches `Đường`). **Built** (8.3 to 8.5). **Location** works natively on the real 325 placed pictures: the map's photo layer uses a box query on the `(Lat, Lon)` index (118 rows examined for a 0.1 degree box), and nearest-first works with MySQL's `ST_Distance_Sphere` (the nearest to Nui Dat is 1.8 km away; 94 are within 10 km); a spatial index is available if the set grows far beyond a few thousand. **If Elasticsearch sync is ever revisited,** two things were left undecided: what to do with note comments (the old site put them inside each note's document, but the live index has no comments field and never removed deleted ones) and poppies (the old site added each to the honour-roll person's document, with the writer's host address; the live honour-roll index has none), and what else reads these indexes. | Not indexed. | Say if you want the sync back (it is in the branch history, commits 0a79ecf and f261b87). |
| 8.2 | **A gap this exposed, fixed:** a picture added to an incident had no position of its own, so it would never have shown on the map's photo layer or in a location search. It is now placed where its incident is (pictures migrated from the old site carry a position of their own; all 23 attached ones do). | Fixed. | n/a |
| 8.3 | **How the search of notes and pictures works.** The search box now also lists **notes** and **photos** (after bases, people and incidents), with the matched words marked, from one call, `GET /api/community-search?q=&limit=`. It uses MySQL's own full-text indexes on the words of every note version (title and body; a query joins to the approved one) and on the caption and credit of pictures, created by the migration `AddCommunitySearch`. **What a query means:** every word typed must be found, and a word matches anything that *starts* with it (so `claymor` finds `claymores`); words in quotes must appear together in that order; at most 6 terms; accents and case are ignored (`Phuoc` finds `Phước`, `Duong` finds `Đường`); words shorter than three letters and MySQL's own stop words (`the`, `of`, `to` and about 30 more) are dropped, because the index holds none of them (so a search of only those finds nothing); anything that is not a letter or a digit is removed, so nothing typed can act as a search operator. Results are ranked by MySQL's relevance. **Only what the public can see is searched:** a note's approved text (never an edit still waiting for a moderator, and no note that was never approved) and approved pictures. The box shows the best 4 of each and the API can return up to 20, with totals. Picking a note opens its incident on its Notes tab; picking a photo opens it and moves the map to where it was taken. It has its own allowance (120 searches a minute per address), because unlike the report search it does not touch Elasticsearch, and answers are cached for a minute. **It depends on MySQL's settings** for full-text words (minimum word length 3 and the default stop word list, which are the defaults in 8.4); if the cluster's MySQL changes either, `SearchTerms` needs the same change. | As described. | `CommunitySearch.cs`, `search-box.*`. |
| 8.4 | **Not searched, on purpose.** Author names (the old site searched them; a new picture does not record its uploader's name where it would be searched, and searching people by what they contributed was not asked for), file names and tags (pictures now have neither: files are named by their hash and there are no tags), comments, poppies, and anything still waiting for a moderator. | As described. | Say if any of these are wanted. |
| 8.5 | **Finding photos by place.** Besides the map's photo layer (326 migrated photos with a position, plus every new one, which takes its incident's position), an incident's Pictures tab now shows **Photos taken nearby**: up to 8 approved photos within 2 km of the incident, nearest first with how far away each is, leaving out the incident's own (`GET /api/contacts/{id}/nearby-media?radiusKm=&limit=`, 0.1 to 25 km). The database narrows them to a box round the incident using the latitude and longitude index and the distance is then measured exactly. Choosing one opens it on the map. Photos with no position at all (25 of the migrated ones have neither an incident nor a place) can be found by their words but not by place. On the old site the procedures `get_nearest_media_to_point` and `get_incident_media_by_location` did this. | As described. | `IncidentMediaService.NearbyAsync`. |
| 8.6 | **Tests.** The search queries are MySQL's own, so besides the ordinary tests (which use a stand-in with the same meaning) there are four that run the real queries on a real MySQL, in a database they create from the migrations and drop afterwards: they are skipped unless `AVW_TEST_MYSQL` holds a connection string (`Server=127.0.0.1;Port=3307;User=root;Password=root` works with the dev stack). CI now starts a MySQL 8.4 for them; **that CI change has not run on GitHub yet.** Checked also in real Chrome against the real notes and pictures (13 steps, including accessibility of the open results and of the nearby photos). | As described. | n/a |

## Map and control refinements (after Phase 7)

| # | Assumption / gap | Default taken | Change it by |
|---|---|---|---|
| R.1 | **Marker size.** The Layers tab has a "Marker size" list: the same for every incident (as before), or scaled by friendly or enemy force, killed or wounded. Killed and wounded were not in the compact contact list, so `GET /api/contacts` now carries `frKia`, `frWia`, `enKia` and `enWia` (read from `Fr_KIA`, `Fr_WIA`, `En_KIA`, `En_WIA`), about 25% more before compression. Area follows the value (radius grows with its square root), from 0.6 times the usual radius for none recorded to 3 times at the cap. The cap is the 95th percentile of the non-zero values across every contact, not just the filtered ones, so a marker does not change size as filters change and one huge engagement does not shrink the rest. Larger markers are drawn first, so smaller ones stay clickable. The choice is in the link as `size=`. | As described. | `SMALLEST_SCALE`, `LARGEST_SCALE` in `contact-layers.ts`; the percentile in `sizeCap` (`contacts.ts`) |
| R.2 | **Point-of-interest icons.** Fire support bases are a solid triangle and bases a starred square (both from the style guide's "Map symbols"). The style guide has no symbol for the others, so a fire support patrol base is an open triangle, a landing zone is a blue disc with an "H", and any other type is a pennant. The live data holds only FSB (97), FSPB (14) and LZ (2); nothing yet is `Base` or `Other`. The icons are drawn on a canvas at start-up, so they need no image files. Contact markers are unchanged. | As described. | `poi-icons.ts` |
| R.3 | **Following a unit** moved out of the Layers tab. Each unit in an incident's details has a small button that draws that unit's path; the paths show whenever any unit is followed, and a "Following N units" button in the top bar stops them all. At most 6 units at once (as before: more would only tangle), and the button for a seventh is disabled. The step buttons (previous and next incident of a unit) sit beside the unit. It is no longer tied to the units chosen on the Filters tab. Links carry `follow=<unit ids>`; an older `track=1` link still follows the units its filters chose. | As described. | `MAX_SEPARATE_TRACKS` in `track.ts` |
| R.4 | **Basemap list.** The basemap choice is a drop-down with a small abstract picture of each basemap (land, water, roads, and contours where the basemap shows relief), keyed by the basemap's id in configuration (`vintage`, `terrain`, `bright`, `positron`, `dark-matter`). A basemap with any other id gets a plain neutral picture until one is added. It is a custom control (a native list cannot show pictures) with the combobox and listbox roles and full keyboard use. **Not yet checked with a screen reader.** | As described. | `PALETTES` in `basemap-icon.ts` |
| R.5 | **Native controls in the vintage skin** (`web/src/_controls.scss`): checkboxes, radio buttons, drop-downs, the file button and scrollbars, from the style guide's tokens (field fill `#E4DABA`, olive edge, brass on the dark map panels). They remain the browser's own elements. Scrollbars use the standard `scrollbar-color` and `scrollbar-width`, so they are thin and coloured but not otherwise shaped; an attempt to draw a ruled track with `::-webkit-scrollbar` left some map-panel bars unpainted in headless Chrome and was dropped. The open list of a drop-down is drawn by the browser, so it takes only the colours a page is allowed to set. Windows high-contrast mode gets the system's own controls. | As described. | `_controls.scss`; component rules that set their own `select` background or padding would hide the arrow |
| R.6 | **The total in the top bar is gone.** The "x of y contacts" line in the Filters tab remains, and is the only place the count is shown. | As described. | n/a |
| R.7 | **Filter accordion.** Every section starts closed, at most one is open (opening one closes the one before it, and the two slide at the same moment rather than one after the other), and opening and closing are animated. It replaces the native `<details>` so that the slide works in every browser; closed sections cannot be tabbed into or read out. | As described. | `accordion-section.ts` |
| R.8 | **Legend.** At the bottom right, above the map's credit line, in one column with the map controls so they never cover each other (the controls shrink and scroll first). It lists only what is on the map: contacts, the open-incident ring, what marker size means (when chosen), the heatmap scale and its field, each type of point that exists, community photos (when there are any and they are on) and a followed unit's path. It starts open, opens and closes with an animation, and is not remembered between visits. The point icons are drawn by the same code as the map's. | As described. | `map-legend.ts` |
| R.9 | **Operation timeline (Gantt chart).** An arrow at the centre of the timeline's top edge opens it, and the timeline grows to at most 24 rem or 55% of the screen. **The data has no start or end date for an operation, so each runs from its first contact to its last** (244 operations; the middle one lasts 6 days, the longest, "LRRP", 1,882). Because most are too short to click as a bar, the whole row is the click target, and it shows the name and, in the tooltip and to a screen reader, the dates and contact count. Rows are earliest first and share the bar chart's time axis, which is the stretch of time the date filter has picked (see R.10). Clicking an operation chooses it as a filter and clicking it again takes it off; any number can be chosen. It is the same filter as Filters > Operation, so the two stay in step and the link carries it as before. Whether the timeline is open is not kept in the link. Bars span all contacts, not only those the filters leave. | As described. | `operationSpans` in `timeline.ts`; `--timeline-h` in `battlemap.css` |
| R.10 | **Date extents.** The date filter is the stretch of time that both the bar chart and the operation list show, so one drag across the bars (whole days, at least 6 pixels, at least one day) sets it for both, as the slider always did. "Reset zoom" (the old "Whole war" button) puts it back; a drag stops Play. Only the operations that run in that stretch are listed, and a bar that runs past its edge is cut there. The axis is labelled to suit: years, months, or days. There is no mouse-wheel zoom, since each step of a wheel would change the filter. | As described. | `dragRange`, `axisTicks` in `timeline.ts` |
| R.11 | **An open incident and the operation list.** While an incident is open, the list shows the period of its operation (first contact to last, with a 10% margin of at least a week, and never under two weeks), scrolls so that operation's row is in the middle, marks the row, and flags the incident's date on the axis and as a line down the list. An incident with no operation gets 30 days either side, and no row is marked. This changes only what the list shows, not the date filter, and it does not open a closed timeline (open it and it is already there). Closing the incident, by the cross or by clicking empty map, puts the list back on the date extents. | As described. | `focusWindow` in `timeline.ts` |
| R.12 | **Empty map closes a panel.** A click on the map where none of the contacts, bases, photos or photo groups is under the pointer closes the incident panel and also a base or a photo panel (the same pattern), but not a person from the honour roll, which is not a place on the map. A drag to pan is not a click. | As described. | `clearMapSelection` in `battlemap.ts` |
| R.13 | **The operation list follows the filters.** It lists only operations that have contacts left by the filters in the Filters tab (a unit, a task, a data source, strengths, mine incidents, hours, report words), and a bar covers only the time those contacts fall in. It ignores two filters: the operation choice itself (or once one operation was chosen no other could be added) and the dates (it has its own axis, and needs to show an incident's whole operation even if the dates exclude it). | As described. | `operationScope` in `battlemap.ts` |
| R.14 | **The map zooms to the contacts a filter leaves.** After any change to a filter (the Filters tab, the timeline, a chip, an operation chosen on the list, or Clear all) the map eases to fit the contacts that are left, no closer than zoom 13, clear of the panels and the timeline. Clearing filters zooms out to every contact, which may be wider than the view before. It does not move when the page opens with filters in its link (the link has a view of its own), when a filter leaves nothing, or while Play is running (a camera that moved with every step would be dizzying). A report search zooms when its answer arrives. | As described. | `FIT_MAX_ZOOM`, `mapPadding` in `battlemap.ts` |
| R.15 | **Photos on the map.** From zoom 14 the community photos are drawn on the map as small framed prints (a paper mount, a thin edge and a shadow), fading in as the plain marker under each fades out, and fully there at zoom 15, where they no longer group (groups now break up at 14, not 15). A print is 50 pixels on its long edge, about three quarters of the strip thumbnails they replace (**an assumption: "the size of what they currently are" was read as those, 67 pixels; change `PHOTO_THUMB_PX`**). Up to zoom 18 they keep that size; from 18 they grow in a straight line, half their size again for each level (3 times at zoom 22). Zoom levels are the map library's (the link's `at=` zoom is one higher). A click opens the photo, as the plain marker did, and a photo opened from a search or a list is flown to at zoom 15 so that it shows in full. **Nothing is fetched until it is about to be seen:** a thumbnail is loaded only when the map first needs it, so only for photos in view at zoom 14 or more, and is kept for the visit. There is no strip and no index; photos come from the list the map already loads from MySQL. That list is capped at 500 (`MaxInArea`, about 325 exist). | As described. | `photo-layers.ts`, `photo-thumbnails.ts` |
| R.16 | **Upload details in the picture panel.** A picture's own page now shows who added it, when it was added, where it was taken, its size in pixels and bytes, and its kind of file, besides the caption, credit, date taken, likes and incident it already showed. **"Added by" is the name shown on the old site for a migrated picture (its author's display name), or the display name of whoever uploaded it; it is the same name notes are already shown under, so it is public.** No email or account id is sent. Lists (an incident's pictures, the map's) do not carry who added a picture. | As described. If names should not show, drop `AddedBy` in `CommunityServices.cs`. | `IncidentMediaView` |
| R.17 | **Full screen.** A "View fullscreen" button (and a click on the picture) shows the full-size photo over the whole window with its caption and credit, and asks the browser for true full screen as well where that is allowed. A browser with no full screen (an iPhone's) still gets the full-window view. Escape, the close button or a click beside the photo closes it, and focus goes back to the picture. "Open in a new tab" is kept beside it. | As described. | `picture-panel.ts` |
| R.18 | **Search by kind.** The search box has a magnifier at the left and, at its right, a menu of the kinds of thing to find: bases, the honour roll, incident reports, incident notes and photos. Any number can be ticked, and none ticked means all of them. A kind that is not ticked is not asked for, so a search of notes and photos never calls the incident-report or honour-roll endpoints (which are Elasticsearch). Ticking a kind searches again at once, the menu sits above the results so the effect shows, and the button turns brass while the search is limited. The native clear button is replaced by one of its own. | As described. | `search-box.ts` |
| R.19 | **Tabs and a fly-out at the left.** Charts moved out of the top bar into a rail of vertical-text tabs at the top left; a tab flies its tool out beside the rail (pressing the open tab, Escape or the tool's own close button puts it away). Charts and the nominal roll are the first two; a new tool is a new entry in `leftTabs` and a new case in the fly-out. The nominal roll lists the roll of those who died in service (522 people, from the same endpoint the honour roll uses; see R.20), 20 at a time, searchable by name or service number, and restrictable by service, rank and corps. Choosing a person shows the existing honour panel inside the fly-out: portrait, rank, corps, birth and death, tours, linked incidents and poppies, with a way back to the list as it was. **Portraits appear only where a `portraits/<service number>.jpg` file is on the media volume (runbook step 6); the dev data has none, so a silhouette stands in.** Links: `charts=1` still opens the charts, `roll=1` the roll. | As described. | `left-tabs.ts`, `nominal-roll.ts` |
| R.20 | **The honour roll moves from Elasticsearch to MySQL, and the roll is now listed and filtered there.** You asked to stop asking Elasticsearch for the nominal roll (a join to tributes and poppies is cheaper in the same database) and to retire `avw_nomroll` in the end, and to use a full-text index rather than caching or sorting in the application. The 522 people who died are copied once by `Avw.Migrator import-roll` (a Helm import job with `import.command=import-roll`; `runbook.md` step 3a) into a table, `honour_roll`, with a MySQL **full-text index** on the words of their names and service number. The API never reads `avw_nomroll` for the roll again, and nothing is kept in memory: MySQL finds, sorts, counts, filters and pages. The import is repeatable (people are matched on service number and only written when they differ) and **never deletes**, so the table can be corrected or added to by other means once Elasticsearch is gone. **Assumptions:** (1) *what the full-text index can find.* InnoDB does not index words under three letters or its stop words, and "will" is one, so "will" for William would find nobody. Those words are looked for in the stored text instead (`LIKE`), which at this size costs nothing; every other word uses the index, with a trailing star so that "wil" finds William, Will and Williams. Every word typed must start some word of a name or the service number, in any name. (2) *Order:* by surname, then given names, ignoring case, by a stored lower-case key. (3) *Names:* the list writes "Surname, First Second Third"; a given name held wholly in capitals in the old roll ("DENNIS ERIC", about a third of them) is set as "Dennis Eric". Everywhere else the name still reads "First Second Third Surname". (4) *Service* (Army, Navy, Air Force) is **not in the old roll** and is worked out on import from the corps and rank: corps Seaman, Naval Airman, Electrical or Supplementary List Seaman Branch is Navy; General Duties is Air Force; otherwise ranks such as Aircraftman, Pilot Officer or Flight Lieutenant are Air Force, and Seaman, Petty Officer, Sub-Lieutenant or Commander are Navy; anything else with a rank or corps is Army; **someone with neither has no service** (about 3 in 500) and is left out of the service list. Corps is the old "branch" (22 values; 18 people have none). Rank is as recorded, and the list sorts "(Temporary) Captain" beside "Captain". (5) *The drop-downs are counted against each other:* each list shows what is left with the search and the other two choices, with a count, so choosing Navy leaves the Navy's ranks; a choice already made stays in its list. **What still reads `avw_nomroll`: the three personnel charts** (they need everyone on the roll, not only those who died; 60,799 records) **and nothing else.** Retiring the index means moving those into MySQL too. | Everything above. | `HonourRollStore`, `HonourRollRows`, `RollImporter`, migration `AddHonourRoll` |
| R.21 | **Hardened images, none of them Ubuntu.** You asked for hardened images, not Ubuntu, and Microsoft's own images for the .NET services. **Shipped images:** the server image now builds on `mcr.microsoft.com/dotnet/sdk:10.0-azurelinux3.0` and runs on `mcr.microsoft.com/dotnet/aspnet:10.0-azurelinux3.0-distroless` (Azure Linux 3.0; no shell, no package manager, non-root UID 1654; 269 MB against 291 MB before). The web image builds on `node:24-trixie-slim` and runs on `gcr.io/distroless/nodejs24-debian13:nonroot` (Google's distroless Node, on Debian: **Microsoft publishes no Node image, so this is my choice; say if you prefer another**, for example Docker Hardened Images or Chainguard, which need an account); it runs as UID 65532, so the web chart's `runAsUser` changed from 1000. Both were built and run here: the API, worker and migrator start, ImageMagick's native library needs only glibc (present), and the web image answers `/healthz` with a read-only root filesystem. The distroless server image's entry point is `dotnet`, so its `CMD` is now just the DLL; the chart's `command: ["dotnet", ...]` is unaffected. Nothing may need a shell in a pod. **CI:** the server, web and Helm jobs now run *inside* those images (Helm and its tools from Azure Linux's own repository, which is Helm 4; the charts lint, render and validate under it), the MySQL service is reached by name, and the image job checks that the images have no shell and do not run as root, that the migrator starts, and that the web image serves its health check. `kubeconform` is pinned by checksum. **Not done, and cannot be on GitHub-hosted runners:** `runs-on: ubuntu-latest` is still the machine that starts the containers, and the image job needs Docker on it; a self-hosted runner on a hardened OS is the way to change that. The old WordPress `Dockerfile` at the repository root (bitnami) is untouched. **The workflow has not been run on GitHub**; its commands were run here in the same images. | As described. | `.github/workflows/ci.yaml`, `server/Dockerfile`, `web/Dockerfile` |
| R.22 | **Stacked pictures spring apart.** Where more than one picture lies under the pointer, a click spreads them out round the spot on thin legs, springing out a little past their place and settling (at once for anyone who asks for less motion): up to 8 round a circle, more along a spiral, always a thumbnail apart and shown as thumbnails whatever the zoom. Choosing one opens it. A numbered group whose pictures would still lie on each other as thumbnails (all within 40 pixels at zoom 15, about 95 m on the ground at this latitude) springs apart where it is instead of zooming in; a group spread wider zooms in as before. The spread thumbnails grow with the zoom exactly as single ones do (their usual size to zoom 18, then half as big again for each level), and the spread widens with them, so it stays open while the map is zoomed. It closes when empty map is clicked, when photos are switched off, and when a picture is added; one spread from a numbered group also closes when the zoom passes a whole level, because the map numbers its groups afresh at each level. **Assumption:** a stack is "drawn on top of each other", measured on screen (3 pixels round the pointer), not a fixed distance on the ground. **Stack badges:** from zoom 14, a thumbnail with others hidden under it carries a small smoke-yellow disc on its top right corner saying how many pictures are there; clicking the disc springs them apart. Pictures count as a stack when their middles are within 24 pixels of each other at the zoom shown (chained; the distance grows with the thumbnails from zoom 18), worked out again each time a zoom ends, so a badge goes once zooming in separates them. The badge of a spread stack is hidden while it is spread. It has no legend entry. | As described. Change `STACK_PX`, `STACK_PROBE_PX`, `SPIDER_SPACING_PX`, `STACK_BADGE_PX`. | `photo-spider.ts`, `photo-stacks.ts` |
| R.23 | **Pictures open in a viewer dialog, not a panel.** Every picture (a map thumbnail, a spread-out one, one in an incident's Pictures tab, a search result, the Images panel) opens in a modal dialog: the full picture on the left, and in a side panel its caption, credit, date taken, who added it and when, place, size and kind of file, likes, a like button and a way to its incident. **This replaces R.16's right-hand panel and R.17's overlay.** It sits over whatever is open and leaves it open, so closing a picture opened from an incident goes back to the incident (before, opening a picture closed the incident). Its full-screen button fills the screen with the picture in the page (true full screen where the browser allows it, the whole window where it does not); Escape leaves full screen first, then the dialog. **Stepping through a set:** arrows either side of the picture (and the left and right arrow keys) step to the previous and next picture, with "2 of 5" above; this works in full screen too, which stays on. The set is the picture's incident's pictures (as that incident lists them) followed by any other approved pictures placed within 30 m of it, each once; it is worked out when the dialog opens and kept while stepping, and each step rings the picture on the map and goes into the link. **Assumption:** "the same location" is within 30 m (`SAME_PLACE_METRES`); the set does not wrap round from the last to the first. **Swiping:** the pictures slide from one to the next (0.3 s), and can be swiped with a finger or dragged with the mouse: the picture follows, and letting go past a fifth of the width, or with a flick, goes on to the neighbour; otherwise it springs back, and past the first or last it moves only a little. A mostly up-and-down move is left to the page. Anyone whose system asks for less motion gets the change at once. **"Open in a new tab" is gone**, and an incident's pictures no longer link to the file in a new tab. `?picture=` now opens for any picture id, over a linked incident too, and one not on the map (waiting for approval, linked from the Studio's new "Placed on the map" link) opens for its uploader or an editor, with the map moved to its place. | As described. | `picture-viewer.ts`, `map-selection.service.ts` |
| R.24 | **An Images tab at the left.** The third tool in the rail (`images=1` in a link). It lists approved pictures newest first, 24 at a time, and narrows to those whose caption or credit has every word typed, using the same MySQL full-text index as the search box (`GET /api/community-media/search`). A Sort drop-down beside the count orders it by newest or oldest added, or by the date taken, latest or earliest first (images with no date taken last), or, while searching, by best match (the default then; newest added otherwise); each card shows its credit and date taken. Resting the mouse on a card (for a third of a second) or tabbing to it shows a tooltip with what the card cuts short: the whole caption, the credit, the date taken, and the incident it belongs to (not its position). It goes when the pointer or focus leaves or the list scrolls. Choosing an image opens it in the viewer **without moving the map**; the viewer's *Go to incident* (for an image of an incident) or *Locate on map* (for one placed on its own) closes it and takes the map there. The search box has a magnifier in it, and *Upload* sits beside it. The panel is the fly-out's full height from the start, so it does not grow while sliding in. An *Upload* button above the list swaps it for a form (and *Back to the images* swaps back), which takes a file, a caption, a credit and a date taken, and a place: the member drags a pin from the panel onto the map (or presses it to drop it in the middle of the view), then drags the pin on the map until it is right. The pin shows the chosen picture. It is sent only once it has a file and a place, and the pin goes when it is sent, when the form is started again, or when the panel closes. | As described. | `pictures-panel.ts`, `picture-placement.service.ts` |
| R.25 | **Pictures placed anywhere, belonging to no incident.** `POST /api/community-media` takes the picture with `lat` and `lon` (decimal degrees, stored to six places, about 10 cm). It needs a signed-in member, the same upload limits and rate limit as an incident's pictures, and waits for an editor like them (an editor's own appears at once, and goes straight onto the map). The editors' email says it was placed on the map and links to it. The same file placed again returns the picture already there rather than a second copy (as an incident's pictures do). **Not done:** attaching a placed picture to an incident later, and moving a picture once sent. | As described. | `IncidentMediaService.PlaceAsync` |
| R.26 | **Icons on the Battle Map's buttons.** Buttons that do something carry a small line icon before their words, from one shared set (`app-icon`), drawn in the colour of the words: try again, show more, back, clear, add an image, send, start again, view, save, cancel, edit, history, delete, approve, reject, comment, add note, remove, like (a heart, filled once liked), view full screen, go to incident, locate on map, play and pause, reset zoom, search everything. **Left without:** tabs, the "Sign in" and other inline links, list rows (people, images, search results), filter chips, and drop-down toggles, which are not buttons of this kind; the poppy button keeps its own poppy. The icons are the project's own drawings, not a library. | As described. | `icon.ts` |
| R.27 | **Back and Forward move between map states.** The map has always kept its state in the URL; now a change to anything but the camera makes a new entry in the browser's history: opening or closing an incident, a base, a person, an image in the viewer (each image stepped to in the viewer is an entry too) or a tool at the left, and changing a filter, the basemap, terrain, an overlay, a layer, the heatmap field, the marker size or the units followed. Panning and zooming only update the entry the reader is on, so each entry keeps where the map was when they moved on. **While the timeline plays** its date steps update the entry rather than adding one each. Back or Forward shows that entry again: the camera eases to where it was (nothing is flown to or fitted), the basemap and layers, filters (a changed report search runs again), followed units, the tool at the left, and what was open, are put back. The first write after Back or Forward only settles the entry arrived at, so it never throws away what is Forward of it. **Assumption:** every settled change is an entry, as a search engine's results pages are; typing in the report search makes one when typing pauses. | As described. | `map-view-state.service.ts`, `restore` in `battlemap.ts` |
| R.28 | **The browser remembers what was left shut.** Whether the Layers/Filters panel is hidden and whether the legend is closed are kept in the browser's local storage (`avw.battlemap.panelOpen`, `avw.battlemap.legendOpen`) and are as they were left on the next visit; a first visit has both open, except on a phone-narrow screen (under 45rem, the map's own breakpoint), where both start shut so the map is not covered. They are per browser and not in the link, so a shared link opens as the reader who follows it last left their own map. Where the browser keeps nothing (storage off or full), they work for the visit and are not remembered. | As described. | `stored-flag.ts` |
| R.29 | **The Battle Map on a phone.** Under 45rem wide: the link home is an arrow button (its name is still "Australia's Vietnam War" for screen readers); beside it a Search button opens the search box underneath (cursor in it, drawn over whatever is there) and a pick puts it away again; Layers and Filters are tabs on a rail at the right edge, as Charts, Nominal roll and Images are at the left, and their panel flies out between the two rails. The timeline and Play are not shown, so the date range can only be changed from a wider screen or a link. The Charts panel leaves out the "Show the figures as a table" section (time charts have no zoom slider on any screen; they zoom by dragging or pinching); the table stays on larger screens, where it is how a screen reader reaches the figures. Larger screens are otherwise unchanged. | As described. | `battlemap.css` (narrow-screen styles), `battlemap.html` |
| R.30 | **The heatmap starts off.** The Battle Map opens with incident markers and no heatmap; the heatmap is switched on under Layers. In the link, `?heatmap=1` now means on and no parameter means off; an older link's `?heatmap=0` still opens with it off, but an older link that relied on the heatmap being on by default now opens without it. | As described. | `battlemap.ts` |
| R.31 | **No credits line on the map.** At the owner's request the map shows no attribution control. The basemaps are built from OpenStreetMap data (OpenMapTiles schema), whose licence (ODbL) asks for a visible credit where the map is shown, and imagery and elevation sources may ask the same. **Open:** put the credits somewhere else on the site (an About or credits page), or bring the control back. Each source's `attribution` is still configured, so the control can be restored in `basemap.service.ts`. | Open: where the credits go. | `basemap.service.ts` |
| R.32 | **Installable as an app.** `site.webmanifest` asks for its own window (`display: standalone`, scope and start `/`) and `index.html` carries Apple's web-app tags with `viewport-fit=cover`, so from the Home Screen the site opens without the browser's bars; the Battle Map and the site header keep clear of the notch, the status bar and the home indicator (safe-area insets). There is no service worker (installing no longer needs one), so it does not work offline. A one-time hint on a phone says how to add it (Safari's Share on an iPhone; Chrome's own Install on Android) and is not shown again once put away. **Open, to try on a real iPhone:** that Keycloak sign-in hands back to the installed app; the installed app has its own storage (remembered panels, sign-in) apart from Safari's. | As described; the sign-in round trip is untested on a device. | `site.webmanifest`, `index.html`, `layout/install-hint.ts` |
| R.33 | **Article bodies are Markdown.** Articles and pages are kept as Markdown (CommonMark with GitHub's tables, strikethrough and bare links), and so are their revisions; the HTML readers get is rendered from it when it is saved (Markdig) and sanitised as before, so pages, feeds and search are unchanged. A video embed, which Markdown cannot say, is a line of HTML the sanitiser allows only for YouTube and Vimeo. The Studio edits visually (the default) or as Markdown, and remembers the choice. There is no underline (Markdown has none). The migrator converts bodies still in HTML once, after the schema migrations (`ArticleBodyConverter`). | As described. | `Avw.Core/Content/ArticleMarkdown.cs`, `studio/rich-text.ts` |

## Later phases

Added as the work proceeds.
