<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Tools for the site's Features (Unit Histories, later Operation Histories): run from a developer's machine to build the files
    committed under content/features/. Never part of the site. Reads the live data read-only.
  -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Hosting" />
    <ProjectReference Include="..\Avw.Api\Avw.Api.csproj" />
    <ProjectReference Include="..\Avw.Data\Avw.Data.csproj" />
  </ItemGroup>

</Project>
